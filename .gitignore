.vscode/
dist/
node_modules/
manifest2.back
/src/static/referenceDocs.txt
.env.secrets
coverage/
.tsbuildinfo

# Puppeteer cache (if browser download is re-enabled)
.cache/

# PEM files containing private keys
keys/*.pem
*.pem
# Ensure no PEM files are ever committed again

# TypeScript build cache
.tsbuildinfo

# Bundle analyzer output
webpack-stats.json

# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~
