# Puppeteer Configuration
# Skip browser download since we use system Chrome for extension testing
PUPPETEER_SKIP_DOWNLOAD=true

# Optional: Custom cache directory for Puppeteer downloads (if needed)
# PUPPETEER_CACHE_DIR=.cache/puppeteer

# E2E Test Configuration
# EXTENSION_BUILD=dev
# HEADLESS=false
